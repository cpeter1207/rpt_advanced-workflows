name: Publish quality images
on:
  workflow_dispatch:
    inputs:
      target_debian:
        description: Debian release for this explicit manual image build.
        type: choice
        required: true
        default: '13'
        options: ['13', '12']
permissions:
  contents: read
  packages: write
concurrency:
  group: quality-image-publication
  cancel-in-progress: false
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        arch: [amd64, arm64]
        include:
          - arch: amd64
            runner: ubuntu-24.04
          - arch: arm64
            runner: ubuntu-24.04-arm
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Remove stale test containers
        run: |
          docker ps -aq --filter label=rpt_advanced.test=true | xargs -r docker rm -f
      - uses: actions/checkout@v7
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: containers/quality.Dockerfile
          platforms: linux/${{ matrix.arch }}
          build-args: DEBIAN_VERSION=${{ inputs.target_debian }}
          load: true
          tags: ghcr.io/cpeter1207/rpt-advanced-quality-debian${{ inputs.target_debian }}:${{ github.sha }}-${{ matrix.arch }}
          cache-from: type=gha,scope=quality-${{ inputs.target_debian }}-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=quality-${{ inputs.target_debian }}-${{ matrix.arch }}
      - name: Verify prepared environment and publish architecture image
        env:
          IMAGE: ghcr.io/cpeter1207/rpt-advanced-quality-debian${{ inputs.target_debian }}:${{ github.sha }}-${{ matrix.arch }}
          ARCH: ${{ matrix.arch }}
        run: |
          docker run --rm --label rpt_advanced.test=true -e ARCH "$IMAGE" sh -c '
            test "$(dpkg --print-architecture)" = "$ARCH" &&
            test -f /usr/include/asterisk.h &&
            asterisk -V && ffmpeg -version &&
            cc --version && clang-format --version && clang-tidy --version &&
            cppcheck --version && doxygen --version && ruff --version && gcovr --version
          '
          docker push "$IMAGE"
      - name: Remove test containers left by interruption
        if: always()
        run: |
          docker ps -aq --filter label=rpt_advanced.test=true | xargs -r docker rm -f
  publish:
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish verified native-platform manifest
        env:
          IMAGE: ghcr.io/cpeter1207/rpt-advanced-quality-debian${{ inputs.target_debian }}
          REVISION: ${{ github.sha }}
        run: |
          docker buildx imagetools create -t "$IMAGE:latest" \
            "$IMAGE:$REVISION-amd64" "$IMAGE:$REVISION-arm64"
          docker buildx imagetools inspect "$IMAGE:latest"
